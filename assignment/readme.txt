readme.txt
assignment documentation

Explanations:

Section 1: Common elements:

-All pages have an element called "<links>", which contains everything that has been imported, such as fonts for the stylesheet, the related stylesheet called: "style.css", the bootstrap link via "cdn.jsdelivr.net".
-All pages except "HomePage.html" include: a header containing the links to all the pages, the client logo, and a "Hello, {user}" element if the user is logged in; a footer with contact information (in the form of a link to a "contact us" page), addresses.

Section 2: Home:



